# Backend Environment Variables
# Copy this file to backend/.env and fill in actual values

# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:5173

# Environment mode (development or production)
ENVIRONMENT=development

# Note: Session secrets are generated at runtime using secrets.token_urlsafe(32)
# No SECRET_KEY environment variable is needed

# Email Configuration for Weekly Summaries
# For Gmail: Use App Password (not regular password)
# Enable 2FA and generate at myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=Activity Tracker <noreply@activitytracker.com>
SMTP_USE_TLS=true
APP_URL=http://localhost:5173

FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Expose Vite dev server port
EXPOSE 5173

# Note: CMD is overridden in docker-compose.dev.yml
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
